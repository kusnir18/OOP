package src.controllers.shared_gettersAndSettres;

import javafx.scene.control.TextArea;

/**
 * <h1>interface PossesDisplay</h1>
 * When is interface implemented controller has a these methods:
 * {@code
 *     TextArea getDisplay();
 *     void setDisplay(TextArea display);
 * }
 * @author  Maros Kusnir
 * @version 1.0
 * @since   2019
 */

public interface PossesDisplay {

    TextArea getDisplay();
    void setDisplay(TextArea display);
}
